module jumptable_arm;

import util;
import abstracthw.cpu;
import abstracthw.memory;
import core.bitop;
template exec(T : IARM7TDMI) {
void entry_00000000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint operand = cpu.read_reg(get_nth_bits(opcode, 8, 12));
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 0, 4)) * cpu.read_reg(get_nth_bits(opcode, 8, 12));
            
            
            if (operand >> 8 == 0xFFFFFF) {
            auto masked_op = operand & 0xFF;
            if (masked_op >= 0xC0) cpu.set_flag_C(false);
            cpu.set_flag_C((masked_op & 0x55) != 0);
            }
            if (operand >> 16 == 0xFFFF) {
            auto masked_op = operand & 0xFFFF;
            if (masked_op >= 0xC000) cpu.set_flag_C(false);
            cpu.set_flag_C((masked_op & 0x5555) != 0);
            }
            if (operand >> 24 == 0xFF) {
            auto masked_op = operand & 0xFFFFFF;
            if (masked_op >= 0xC00000) cpu.set_flag_C(false);
            cpu.set_flag_C((masked_op & 0x555555) != 0);
            }
            else {
            cpu.set_flag_C((operand >> 30) == 2);
            }
            int m = 4;
            if      ((operand >> 8)  == 0x0 || (operand >>> 8)  == 0xFFFFFF) m = 1;
            else if ((operand >> 16) == 0x0 || (operand >>> 16) == 0xFFFF)   m = 2;
            else if ((operand >> 24) == 0x0 || (operand >>> 24) == 0xFF)     m = 3;
            for (int i = 0; i < m; i++) cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            cpu.memory.write_halfword(address, cpu.read_reg(get_nth_bits(opcode, 12, 16)) & 0xFFFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_00000001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint operand = cpu.read_reg(get_nth_bits(opcode, 8, 12));
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 0, 4)) * cpu.read_reg(get_nth_bits(opcode, 8, 12));
            cpu.set_flag_Z(cpu.read_reg(get_nth_bits(opcode, 16, 20)) == 0);
            cpu.set_flag_N(cpu.read_reg(get_nth_bits(opcode, 16, 20)) >> 31);
            if (operand >> 8 == 0xFFFFFF) {
            auto masked_op = operand & 0xFF;
            if (masked_op >= 0xC0) cpu.set_flag_C(false);
            cpu.set_flag_C((masked_op & 0x55) != 0);
            }
            if (operand >> 16 == 0xFFFF) {
            auto masked_op = operand & 0xFFFF;
            if (masked_op >= 0xC000) cpu.set_flag_C(false);
            cpu.set_flag_C((masked_op & 0x5555) != 0);
            }
            if (operand >> 24 == 0xFF) {
            auto masked_op = operand & 0xFFFFFF;
            if (masked_op >= 0xC00000) cpu.set_flag_C(false);
            cpu.set_flag_C((masked_op & 0x555555) != 0);
            }
            else {
            cpu.set_flag_C((operand >> 30) == 2);
            }
            int m = 4;
            if      ((operand >> 8)  == 0x0 || (operand >>> 8)  == 0xFFFFFF) m = 1;
            else if ((operand >> 16) == 0x0 || (operand >>> 16) == 0xFFFF)   m = 2;
            else if ((operand >> 24) == 0x0 || (operand >>> 24) == 0xFF)     m = 3;
            for (int i = 0; i < m; i++) cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00000010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint operand = cpu.read_reg(get_nth_bits(opcode, 8, 12));
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 0, 4)) * cpu.read_reg(get_nth_bits(opcode, 8, 12)) + cpu.read_reg(get_nth_bits(opcode, 12, 16));
            cpu.run_idle_cycle();
            
            if (operand >> 8 == 0xFFFFFF) {
            auto masked_op = operand & 0xFF;
            if (masked_op >= 0xC0) cpu.set_flag_C(false);
            cpu.set_flag_C((masked_op & 0x55) != 0);
            }
            if (operand >> 16 == 0xFFFF) {
            auto masked_op = operand & 0xFFFF;
            if (masked_op >= 0xC000) cpu.set_flag_C(false);
            cpu.set_flag_C((masked_op & 0x5555) != 0);
            }
            if (operand >> 24 == 0xFF) {
            auto masked_op = operand & 0xFFFFFF;
            if (masked_op >= 0xC00000) cpu.set_flag_C(false);
            cpu.set_flag_C((masked_op & 0x555555) != 0);
            }
            else {
            cpu.set_flag_C((operand >> 30) == 2);
            }
            int m = 4;
            if      ((operand >> 8)  == 0x0 || (operand >>> 8)  == 0xFFFFFF) m = 1;
            else if ((operand >> 16) == 0x0 || (operand >>> 16) == 0xFFFF)   m = 2;
            else if ((operand >> 24) == 0x0 || (operand >>> 24) == 0xFF)     m = 3;
            for (int i = 0; i < m; i++) cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            cpu.memory.write_halfword(address, cpu.read_reg(get_nth_bits(opcode, 12, 16)) & 0xFFFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_00000011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint operand = cpu.read_reg(get_nth_bits(opcode, 8, 12));
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 0, 4)) * cpu.read_reg(get_nth_bits(opcode, 8, 12)) + cpu.read_reg(get_nth_bits(opcode, 12, 16));
            cpu.run_idle_cycle();
            cpu.set_flag_Z(cpu.read_reg(get_nth_bits(opcode, 16, 20)) == 0);
            cpu.set_flag_N(cpu.read_reg(get_nth_bits(opcode, 16, 20)) >> 31);
            if (operand >> 8 == 0xFFFFFF) {
            auto masked_op = operand & 0xFF;
            if (masked_op >= 0xC0) cpu.set_flag_C(false);
            cpu.set_flag_C((masked_op & 0x55) != 0);
            }
            if (operand >> 16 == 0xFFFF) {
            auto masked_op = operand & 0xFFFF;
            if (masked_op >= 0xC000) cpu.set_flag_C(false);
            cpu.set_flag_C((masked_op & 0x5555) != 0);
            }
            if (operand >> 24 == 0xFF) {
            auto masked_op = operand & 0xFFFFFF;
            if (masked_op >= 0xC00000) cpu.set_flag_C(false);
            cpu.set_flag_C((masked_op & 0x555555) != 0);
            }
            else {
            cpu.set_flag_C((operand >> 30) == 2);
            }
            int m = 4;
            if      ((operand >> 8)  == 0x0 || (operand >>> 8)  == 0xFFFFFF) m = 1;
            else if ((operand >> 16) == 0x0 || (operand >>> 16) == 0xFFFF)   m = 2;
            else if ((operand >> 24) == 0x0 || (operand >>> 24) == 0xFF)     m = 3;
            for (int i = 0; i < m; i++) cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00000100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            cpu.memory.write_halfword(address, cpu.read_reg(get_nth_bits(opcode, 12, 16)) & 0xFFFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_00000101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00000110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            cpu.memory.write_halfword(address, cpu.read_reg(get_nth_bits(opcode, 12, 16)) & 0xFFFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_00000111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00001000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            ulong result = (cast(ulong) cpu.read_reg(get_nth_bits(opcode, 0, 4))) *
            (cast(ulong) cpu.read_reg(get_nth_bits(opcode, 8, 12)));
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cast(uint) (result & 0xFFFFFFFF);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cast(uint) (result >> 32);
            int m = 4;
            int operand = cpu.read_reg(get_nth_bits(opcode, 8, 12));
            if      ((operand >> 8)  == 0x0) m = 1;
            else if ((operand >> 16) == 0x0) m = 2;
            else if ((operand >> 24) == 0x0) m = 3;
            for (int i = 0; i < m + 1; i++) cpu.run_idle_cycle();
            
            
            
            
            
            
            
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            cpu.memory.write_halfword(address, cpu.read_reg(get_nth_bits(opcode, 12, 16)) & 0xFFFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_00001001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            ulong result = (cast(ulong) cpu.read_reg(get_nth_bits(opcode, 0, 4))) *
            (cast(ulong) cpu.read_reg(get_nth_bits(opcode, 8, 12)));
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cast(uint) (result & 0xFFFFFFFF);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cast(uint) (result >> 32);
            int m = 4;
            int operand = cpu.read_reg(get_nth_bits(opcode, 8, 12));
            if      ((operand >> 8)  == 0x0) m = 1;
            else if ((operand >> 16) == 0x0) m = 2;
            else if ((operand >> 24) == 0x0) m = 3;
            for (int i = 0; i < m + 1; i++) cpu.run_idle_cycle();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            cpu.refill_pipeline();
            } else {
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_N(result >> 63);
            }
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00001010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            ulong result = (cast(ulong) cpu.read_reg(get_nth_bits(opcode, 0, 4))) *
            (cast(ulong) cpu.read_reg(get_nth_bits(opcode, 8, 12)));
            result += ((cast(ulong) cpu.read_reg(get_nth_bits(opcode, 16, 20))) << 32) | (cast(ulong) cpu.read_reg(get_nth_bits(opcode, 12, 16)));
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cast(uint) (result & 0xFFFFFFFF);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cast(uint) (result >> 32);
            int m = 4;
            int operand = cpu.read_reg(get_nth_bits(opcode, 8, 12));
            if      ((operand >> 8)  == 0x0) m = 1;
            else if ((operand >> 16) == 0x0) m = 2;
            else if ((operand >> 24) == 0x0) m = 3;
            for (int i = 0; i < m + 2; i++) cpu.run_idle_cycle();
            
            
            
            
            
            
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            cpu.memory.write_halfword(address, cpu.read_reg(get_nth_bits(opcode, 12, 16)) & 0xFFFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_00001011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            ulong result = (cast(ulong) cpu.read_reg(get_nth_bits(opcode, 0, 4))) *
            (cast(ulong) cpu.read_reg(get_nth_bits(opcode, 8, 12)));
            result += ((cast(ulong) cpu.read_reg(get_nth_bits(opcode, 16, 20))) << 32) | (cast(ulong) cpu.read_reg(get_nth_bits(opcode, 12, 16)));
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cast(uint) (result & 0xFFFFFFFF);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cast(uint) (result >> 32);
            int m = 4;
            int operand = cpu.read_reg(get_nth_bits(opcode, 8, 12));
            if      ((operand >> 8)  == 0x0) m = 1;
            else if ((operand >> 16) == 0x0) m = 2;
            else if ((operand >> 24) == 0x0) m = 3;
            for (int i = 0; i < m + 2; i++) cpu.run_idle_cycle();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_N(result >> 63);
            }
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00001100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // unintuitive sign extension
            // http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            const ulong mask = 1U << (32 - 1);
            ulong result = (((cast(long) cpu.read_reg(get_nth_bits(opcode, 0, 4)))  ^ mask) - mask) *
            (((cast(long) cpu.read_reg(get_nth_bits(opcode, 8, 12))) ^ mask) - mask);
            int m = 4;
            int operand = cpu.read_reg(get_nth_bits(opcode, 8, 12));
            if      ((operand >> 8)  == 0x0 || (operand >>> 8)  == 0xFFFFFF) m = 1;
            else if ((operand >> 16) == 0x0 || (operand >>> 16) == 0xFFFF)   m = 2;
            else if ((operand >> 24) == 0x0 || (operand >>> 24) == 0xFF)     m = 3;
            for (int i = 0; i < m + 1; i++) cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cast(uint) (result & 0xFFFFFFFF);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cast(uint) (result >> 32);
            
            
            
            
            
            
            
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            cpu.memory.write_halfword(address, cpu.read_reg(get_nth_bits(opcode, 12, 16)) & 0xFFFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_00001101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // unintuitive sign extension
            // http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            const ulong mask = 1U << (32 - 1);
            ulong result = (((cast(long) cpu.read_reg(get_nth_bits(opcode, 0, 4)))  ^ mask) - mask) *
            (((cast(long) cpu.read_reg(get_nth_bits(opcode, 8, 12))) ^ mask) - mask);
            int m = 4;
            int operand = cpu.read_reg(get_nth_bits(opcode, 8, 12));
            if      ((operand >> 8)  == 0x0 || (operand >>> 8)  == 0xFFFFFF) m = 1;
            else if ((operand >> 16) == 0x0 || (operand >>> 16) == 0xFFFF)   m = 2;
            else if ((operand >> 24) == 0x0 || (operand >>> 24) == 0xFF)     m = 3;
            for (int i = 0; i < m + 1; i++) cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cast(uint) (result & 0xFFFFFFFF);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cast(uint) (result >> 32);
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            cpu.refill_pipeline();
            } else {
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_N(result >> 63);
            }
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00001110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // unintuitive sign extension
            // http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            const ulong mask = 1U << (32 - 1);
            ulong result = (((cast(long) cpu.read_reg(get_nth_bits(opcode, 0, 4)))  ^ mask) - mask) *
            (((cast(long) cpu.read_reg(get_nth_bits(opcode, 8, 12))) ^ mask) - mask);
            result += ((cast(ulong) cpu.read_reg(get_nth_bits(opcode, 16, 20))) << 32) | (cast(ulong) cpu.read_reg(get_nth_bits(opcode, 12, 16)));
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cast(uint) (result & 0xFFFFFFFF);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cast(uint) (result >> 32);
            int m = 4;
            uint operand = cpu.read_reg(get_nth_bits(opcode, 8, 12));
            if      ((operand >> 8)  == 0x0 || (operand >>> 8)  == 0xFFFFFF) m = 1;
            else if ((operand >> 16) == 0x0 || (operand >>> 16) == 0xFFFF)   m = 2;
            else if ((operand >> 24) == 0x0 || (operand >>> 24) == 0xFF)     m = 3;
            for (int i = 0; i < m + 2; i++) cpu.run_idle_cycle();
            
            
            
            
            
            
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            cpu.memory.write_halfword(address, cpu.read_reg(get_nth_bits(opcode, 12, 16)) & 0xFFFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_00001111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // unintuitive sign extension
            // http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            const ulong mask = 1U << (32 - 1);
            ulong result = (((cast(long) cpu.read_reg(get_nth_bits(opcode, 0, 4)))  ^ mask) - mask) *
            (((cast(long) cpu.read_reg(get_nth_bits(opcode, 8, 12))) ^ mask) - mask);
            result += ((cast(ulong) cpu.read_reg(get_nth_bits(opcode, 16, 20))) << 32) | (cast(ulong) cpu.read_reg(get_nth_bits(opcode, 12, 16)));
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cast(uint) (result & 0xFFFFFFFF);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cast(uint) (result >> 32);
            int m = 4;
            uint operand = cpu.read_reg(get_nth_bits(opcode, 8, 12));
            if      ((operand >> 8)  == 0x0 || (operand >>> 8)  == 0xFFFFFF) m = 1;
            else if ((operand >> 16) == 0x0 || (operand >>> 16) == 0xFFFF)   m = 2;
            else if ((operand >> 24) == 0x0 || (operand >>> 24) == 0xFF)     m = 3;
            for (int i = 0; i < m + 2; i++) cpu.run_idle_cycle();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_N(result >> 63);
            }
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00010000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.regs[get_nth_bits(opcode, 12, 16)] = *cpu.cpsr;
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint value   = cpu.memory.read_word(address);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.memory.write_word(address, cpu.read_reg(get_nth_bits(opcode, 0, 4)));
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) cpu.refill_pipeline();
            cpu.run_idle_cycle();
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            cpu.memory.write_halfword(address, cpu.read_reg(get_nth_bits(opcode, 12, 16)) & 0xFFFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_00010001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00010010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0001: {
            cpu.set_bit_T(cpu.read_reg(get_nth_bits(opcode, 0, 4)) & 0x1);
            *cpu.pc = (cpu.read_reg(get_nth_bits(opcode, 0, 4)) & 0xFFFFFFFE);
            cpu.refill_pipeline();
            break;
        }
        case 0b0010: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0011: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0100: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0101: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0110: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0111: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1000: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1001: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1010: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1011: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1100: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1101: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1110: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1111: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
    default: break;
    }
}

void entry_00010011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00010100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.regs[get_nth_bits(opcode, 12, 16)] = *cpu.spsr;
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            uint temp = cpu.memory.read_byte(cpu.read_reg(get_nth_bits(opcode, 16, 20)));
            cpu.memory.write_byte(cpu.read_reg(get_nth_bits(opcode, 16, 20)), cpu.read_reg(get_nth_bits(opcode, 0, 4)) & 0xFF);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = temp;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (get_nth_bits(opcode, 12, 16) == 15) cpu.refill_pipeline();
            cpu.run_idle_cycle();
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            cpu.memory.write_halfword(address, cpu.read_reg(get_nth_bits(opcode, 12, 16)) & 0xFFFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_00010101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00010110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0001: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0010: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0011: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0100: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0101: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0110: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0111: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1000: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1001: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1010: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1011: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1100: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1101: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1110: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1111: {
            uint  operand     = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
    default: break;
    }
}

void entry_00010111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00011000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            cpu.memory.write_halfword(address, cpu.read_reg(get_nth_bits(opcode, 12, 16)) & 0xFFFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_00011001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00011010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            cpu.memory.write_halfword(address, cpu.read_reg(get_nth_bits(opcode, 12, 16)) & 0xFFFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_00011011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            uint  offset = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00011100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            cpu.memory.write_halfword(address, cpu.read_reg(get_nth_bits(opcode, 12, 16)) & 0xFFFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_00011101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00011110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            cpu.memory.write_halfword(address, cpu.read_reg(get_nth_bits(opcode, 12, 16)) & 0xFFFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_00011111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - rs);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else if (rs == 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else if (rs > 32) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = 0;
            }
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            if        (rs == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if (rs < 32) {
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) rs);
            cpu.shifter_carry_out = get_nth_bit(rm, rs - 1);
            } else { // rs >= 32
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            }
            // S and H being true indicates ROR
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint rm = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            uint rs = get_nth_bits(cpu.read_reg(get_nth_bits(opcode, 8, 12)), 0, 8);
            cpu.run_idle_cycle();
            int pc_additional_shift_amount;
            if (get_nth_bits(opcode, 4, 12) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else {
            if (get_nth_bits(opcode,  0,  4) == 15) // are we PC?
            rm += 4;
            if (get_nth_bits(opcode,  8, 12) == 15) // are we PC?
            rs += 4;
            if (get_nth_bits(opcode, 16, 20) == 15)
            pc_additional_shift_amount = 4;
            else
            pc_additional_shift_amount = 0;
            }
            // There's a lot of exceptions to the rules here, because rs can be
            // 32 bits long, and that causes some problems. immediate shift had
            // a limit of 5 bits.
            // S being false and H being false indicates a left shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being false and H being true indicates a right shift
            
            
            
            
            
            
            
            
            
            
            
            
            // S being true and H being false indicates ASR
            
            
            
            
            
            
            
            
            
            // S and H being true indicates ROR
            if        ((rs & 0xFF) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else if ((rs & 0x1F) == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // rs & 0x1F > 0
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) (rs & 0x1F));
            cpu.shifter_carry_out = get_nth_bit(rm, (rs & 0x1F) - 1);
            }
            // _g_cpu_cycles_remaining++;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = rm;
            cpu.shifter_carry_out = cpu.get_flag_C();
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSL(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, 32 - shift_immediate);
            }
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            if (shift_immediate == 0) {
            cpu.shifter_operand   = 0;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            // _g_cpu_cycles_remaining--;
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.LSR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b1) == 0b1) value = ((value & 0xFF) << 24) | (value >> 8);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            if (shift_immediate == 0) {
            cpu.shifter_operand   = get_nth_bit(rm, 31) ? 0xFFFFFFFF : 0x00000000;
            cpu.shifter_carry_out = get_nth_bit(rm, 31);
            } else { // shift_immediate > 0
            cpu.shifter_operand   = cpu.ASR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = sign_extend(cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL), 8);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint rm              = cpu.read_reg(get_nth_bits(opcode, 0, 4));
            // S being false and H being false indicates a left shift... sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            // S being false and H being true indicates a right shift... also sort of. there's a special case when
            // shift_immediate == 0
            
            
            
            
            
            
            
            // S being true and H being false indicates an arithmetic shift.
            
            
            
            
            
            
            // S being true and H being true indicates either ROR or RRX, depending on if
            // shift_immediate is 0
            if (shift_immediate == 0) { // RRX
            cpu.shifter_operand   = cpu.get_flag_C() << 31 | cpu.LSR(rm, 1);
            cpu.shifter_carry_out = get_nth_bit(rm, 0);
            } else { // shift_immediate > 0, ROR
            cpu.shifter_operand   = cpu.ROR(rm, cast(ubyte) shift_immediate);
            cpu.shifter_carry_out = get_nth_bit(rm, shift_immediate - 1);
            }
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            uint addr_writeback;
            bool should_writeback = false;
            // we either get the offset straight from the opcode or from the registers depending on the I bit
            ubyte offset = cast(ubyte)(get_nth_bits(opcode, 0, 4) | (get_nth_bits(opcode, 8, 12) << 4));
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_halfword(address, AccessType.NONSEQUENTIAL);
            if (address & 1) {
            value = sign_extend(value >> 8, 8);
            } else {
            value = sign_extend(value, 16);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = value;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00100000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00100001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00100010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00100011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00100100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00100101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            cpu.set_flag_C((cpu.shifter_operand <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00100110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00100111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cpu.shifter_operand >= register_operand));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            
            
            
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00101000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00101001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand)                    >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00101010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00101011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand + cpu.shifter_operand + cpu.get_flag_C();
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand + cpu.get_flag_C()) >= 0x1_0000_0000);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00101100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00101101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = register_operand - cpu.shifter_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            
            
            cpu.set_flag_C((cast(ulong) cpu.shifter_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1) <= register_operand));
            cpu.set_flag_V(((cpu.shifter_operand >> 31) ^ (register_operand >> 31)) && ((cpu.shifter_operand >> 31) == (result >> 31)));
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00101110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            
            
            
            
            
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00101111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result   = cpu.shifter_operand - register_operand;
            result -= ((~(cast(uint)cpu.get_flag_C())) & 1);
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            // @IF( S)     warning(format("Syncing CPSR... Returning from interrupt. %x", *cpu.spsr));
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            
            
            
            cpu.set_flag_C((cpu.shifter_operand >= cast(ulong) register_operand + cast(ulong) ((~(cast(uint)cpu.get_flag_C())) & 1)));
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) == (result >> 31)));
            
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00110000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            
            
            
            
            
            
            
            break;
        }
    default: break;
    }
}

void entry_00110001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
    default: break;
    }
}

void entry_00110010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0001: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0010: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0011: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0100: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0101: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0110: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0111: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1000: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1001: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1010: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1011: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1100: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1101: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1110: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1111: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            
            
            
            
            
            if (cpu.in_a_privileged_mode()) {
            if (get_nth_bit(opcode, 16)) *cpu.cpsr = (*cpu.cpsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.cpsr = (*cpu.cpsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.cpsr = (*cpu.cpsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            }
            if (get_nth_bit(opcode, 19)) *cpu.cpsr = (*cpu.cpsr & 0x00FFFFFF) | (operand & 0xFF000000);
            if (get_nth_bit(opcode, 16) && cpu.in_a_privileged_mode()) {
            if (bit_T_changed) cpu.refill_pipeline();
            }
            cpu.update_mode();
            // _g_cpu_cycles_remaining++;
            break;
        }
    default: break;
    }
}

void entry_00110011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand ^ cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            break;
        }
    default: break;
    }
}

void entry_00110100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_00110101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + ~cpu.shifter_operand + 1;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) ^ (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C(register_operand >= cpu.shifter_operand);
            }
            // warning(format("Comparing %x with %x", register_operand, cpu.shifter_operand));
            // warning(format("CMP: %x %x, %x %x", register_operand, cpu.shifter_operand, opcode, *cpu.pc));
            break;
        }
    default: break;
    }
}

void entry_00110110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0001: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0010: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0011: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0100: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0101: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0110: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b0111: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1000: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1001: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1010: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1011: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1100: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1101: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1110: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
        case 0b1111: {
            uint  operand     = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) (get_nth_bits(opcode, 8, 12) * 2));
            bool bit_T_changed = get_nth_bit(operand, 5) ^ cpu.get_bit_T();
            if (cpu.has_spsr()) {
            if (get_nth_bit(opcode, 16)) *cpu.spsr = (*cpu.spsr & 0xFFFFFF00) | (operand & 0x000000FF);
            if (get_nth_bit(opcode, 17)) *cpu.spsr = (*cpu.spsr & 0xFFFF00FF) | (operand & 0x0000FF00);
            if (get_nth_bit(opcode, 18)) *cpu.spsr = (*cpu.spsr & 0xFF00FFFF) | (operand & 0x00FF0000);
            if (get_nth_bit(opcode, 19)) *cpu.spsr = (*cpu.spsr & 0x00FFFFFF) | (operand & 0xFF000000);
            }
            
            
            
            
            
            
            
            
            
            // _g_cpu_cycles_remaining++;
            break;
        }
    default: break;
    }
}

void entry_00110111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint old_value        = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result           = register_operand + cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_V(((register_operand >> 31) == (cpu.shifter_operand >> 31)) && ((register_operand >> 31) ^ (result >> 31)));
            cpu.set_flag_C((cast(ulong) register_operand + cast(ulong) cpu.shifter_operand) >= 0x1_0000_0000);
            }
            break;
        }
    default: break;
    }
}

void entry_00111000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00111001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand| cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00111010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00111011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result =  cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00111100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00111101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint register_operand = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            if (get_nth_bits(opcode, 16, 20) == 15) register_operand += pc_additional_shift_amount;
            uint result = register_operand & ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00111110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            
            
            
            
            
            
            
            
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_00111111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            cpu.shifter_operand   = cpu.ROR(get_nth_bits(opcode, 0, 8), cast(ubyte) ((get_nth_bits(opcode, 8, 12)) * 2));
            cpu.shifter_carry_out = get_nth_bits(opcode, 8, 12) == 0 ? cpu.get_flag_C() : get_nth_bit(cpu.shifter_operand, 31);
            // _g_cpu_cycles_remaining++;
            int pc_additional_shift_amount = 0;
            uint result = ~cpu.shifter_operand;
            if (get_nth_bits(opcode, 12, 16) == 15) { // are we register PC?
            *cpu.cpsr = *cpu.spsr;
            cpu.update_mode();
            } else {
            cpu.set_flag_N(result >> 31);
            cpu.set_flag_Z(result == 0);
            cpu.set_flag_C(cpu.shifter_carry_out != 0);
            }
            cpu.regs[get_nth_bits(opcode, 12, 16)] = result;
            if (get_nth_bits(opcode, 12, 16) == 15) {
            // _g_cpu_cycles_remaining += 2;
            cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01000000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01000001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01000010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01000011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01000100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01000101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01000110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01000111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01001000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01001001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01001010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01001011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01001100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01001101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01001110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01001111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            addr_writeback = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            should_writeback = true;
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01010000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01010001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01010010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01010011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01010100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01010101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01010110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01010111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01011000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01011001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01011010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01011011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01011100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01011101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01011110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01011111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            // NOTE: this is the IMMEDIATE OFFSET part of ADDRESSING_MODE_2
            // also yes it does look very similar to ADDRESSING_MODE_3 but what can I realistically do,
            // this was the way the cpu was designed.
            // actually, this can be made cleaner if i allow components to contain components.... maybe
            // a task for another day.
            uint addr_writeback;
            bool should_writeback = false;
            // pc is required to know if we should add an extra offset of 4 to account for the pipeline
            bool is_pc = get_nth_bits(opcode, 16, 20) == 15;
            // this is immediate addressing - so we just get the offset straight from the opcode
            uint offset = get_nth_bits(opcode, 0, 12);
            // U determines if we add or subtract
            // P determines if we're doing post-indexed addressing or not
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + offset;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            
            
            // if the writeback bit is specified, then do a writeback.
            addr_writeback   = address;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01100000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01100001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01100010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01100011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01100100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01100101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01100110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01100111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01101000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01101001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01101010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01101011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01101100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01101101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01101110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01101111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            addr_writeback   = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + index;
            should_writeback = true;
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01110000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01110001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01110010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01110011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01110100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01110101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01110110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01110111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address -= index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01111000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01111001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01111010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_word(address, result, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            // _g_cpu_cycles_remaining += 2;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01111011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            uint value = cpu.memory.read_word(address, AccessType.NONSEQUENTIAL);
            if ((address & 0b11) == 0b01) value = ((value & 0xFF)     << 24) | (value >> 8);
            if ((address & 0b11) == 0b10) value = ((value & 0xFFFF)   << 16) | (value >> 16);
            if ((address & 0b11) == 0b11) value = ((value & 0xFFFFFF) << 8)  | (value >> 24);
            ubyte rd = cast(ubyte) get_nth_bits(opcode, 12, 16);
            if (rd == 15) {
            *cpu.pc = value & 0xFFFFFFFC;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 2;
            } else {
            cpu.regs[rd] = value;
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01111100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01111101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01111110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            
            uint result = cpu.read_reg(get_nth_bits(opcode, 12, 16));
            if (get_nth_bits(opcode, 12, 16) == 15) result += 4;
            cpu.memory.write_byte(address, result & 0xFF, AccessType.NONSEQUENTIAL);
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_01111111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            index = operand << shift_immediate;
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            if (shift_immediate == 0) {
            index = 0;
            } else {
            index = operand >> shift_immediate;
            }
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            if (shift_immediate != 0) {
            if ((operand >> 31) == 1) {
            index = (((1 << shift_immediate) - 1) << (32 - shift_immediate)) | (operand >> shift_immediate);
            } else {
            index = operand >> shift_immediate;
            }
            }
            else index = (get_nth_bit(operand, 31) == 1 ? 0xFFFFFFFF : 0x00000000);
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            
            
            
            
            
            
            
            
            
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint operand = cpu.read_reg(get_nth_bits(opcode, 0,  4));
            uint shift_immediate = get_nth_bits(opcode, 7, 12);
            uint addr_writeback;
            bool should_writeback = false;
            uint index = 0;
            // S being false indicates a shift of some type
            
            
            
            
            
            // S being true but H being false indicates an arithmetic shift
            
            
            
            
            
            
            
            // S being true and H being true indicates a rotate right or a rotate right with extend.
            if (shift_immediate != 0) {
            uint rotated_off = get_nth_bits(operand, 0, shift_immediate);  // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, shift_immediate, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << (32 - shift_immediate));
            }
            else {
            uint rotated_off = get_nth_bit(cpu.get_flag_C(), 0);      // the value that is rotated off
            uint rotated_in  = get_nth_bits(operand, 1, 32); // the value that stays after the rotation
            index = rotated_in | (rotated_off << 31);
            }
            // P bit being set indicates offset addressing or pre-indexed addressing
            address += index;
            // @IF( P  W) cpu.read_reg(get_nth_bits(opcode, 16, 20)) = address;
            addr_writeback   = address;
            should_writeback = true;
            // P bit being clear indicates post-indexed addressing
            
            
            if (get_nth_bits(opcode, 16, 20) == get_nth_bits(opcode, 12, 16)) should_writeback = false;
            cpu.regs[get_nth_bits(opcode, 12, 16)] = cpu.memory.read_byte(address, AccessType.NONSEQUENTIAL);
            if (get_nth_bits(opcode, 12, 16) == 15) {
            cpu.refill_pipeline();
            }
            cpu.run_idle_cycle();
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            if (should_writeback) {
            cpu.regs[get_nth_bits(opcode, 16, 20)] = addr_writeback;
            if (get_nth_bits(opcode, 16, 20) == 15) cpu.refill_pipeline();
            }
            break;
        }
    default: break;
    }
}

void entry_10000000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10000001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10000010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10000011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10000100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10000101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10000110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10000111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4 + 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4 + 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10001000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10001001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10001010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10001011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10001100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10001101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10001110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10001111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10010000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10010001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10010010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10010011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10010100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10010101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10010110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10010111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            
            
            uint start_address     = address - popcnt(opcode & 0xFFFF) * 4;
            
            
            uint writeback_address = address - popcnt(opcode & 0xFFFF) * 4;
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            
            
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 16 * 4;
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) - 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10011000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10011001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10011010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            if (i == 15) {
            cpu.memory.write_word(address, cpu.read_reg(15) + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.read_reg(i), access_type);
            }
            
            
            
            
            
            
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10011011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            cpu.regs[i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            
            
            
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10011100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10011101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10011110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            
            
            cpu.memory.write_word(address, *cpu.pc);
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            
            
            if (i == 15) {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + 15] + 4, access_type);
            } else {
            cpu.memory.write_word(address, cpu.register_file[MODE_USER.OFFSET + i], access_type);
            }
            address += 4;
            
            
            
            
            if (is_first_access) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            is_first_access = false;
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            *cpu.pc &= 0xFFFFFFFC;
            
            cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10011111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b0111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1000: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1001: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1010: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1011: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1100: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1101: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1110: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
        case 0b1111: {
            uint address = cpu.read_reg(get_nth_bits(opcode, 16, 20));
            uint start_address     = address + 4;
            
            
            uint writeback_address = address + popcnt(opcode & 0xFFFF) * 4;
            
            address = start_address;
            if (start_address == 0x07FF_FFF8) {
            warning(format("%x", opcode));
            }
            // this if statement emulates unpredictable ARM7TDMI behavior - empty rlist
            if (get_nth_bits(opcode, 0, 16) == 0) {
            start_address = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4;
            
            address = start_address;
            *cpu.pc = cpu.memory.read_word(address);
            cpu.refill_pipeline();
            cpu.run_idle_cycle();
            cpu.regs[get_nth_bits(opcode, 16, 20)] = cpu.read_reg(get_nth_bits(opcode, 16, 20)) + 4 * 16;
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            return;
            }
            int mask = 1;
            int num_transactions = 0;
            bool pc_loaded   = false;
            bool pc_included = get_nth_bit(opcode, 15);
            bool register_in_rlist = (opcode >> get_nth_bits(opcode, 16, 20)) & 1;
            bool is_first_access = true;
            AccessType access_type = AccessType.NONSEQUENTIAL;
            for (int i = 0;  i < 16; i++) {
            if (opcode & mask) {
            
            
            
            
            
            if (pc_included) cpu.regs         [i]                    = cpu.memory.read_word(address, access_type);
            else             cpu.register_file[MODE_USER.OFFSET + i] = cpu.memory.read_word(address, access_type);
            
            
            
            
            address += 4;
            num_transactions++;
            if (i == 15) {
            pc_loaded = true;
            cpu.refill_pipeline();
            }
            
            access_type = AccessType.SEQUENTIAL;
            }
            mask <<= 1;
            }
            // TODO: does pipeline reset when empty rlist?
            cpu.pipeline_access_type = AccessType.NONSEQUENTIAL;
            cpu.run_idle_cycle();
            *cpu.pc &= 0xFFFFFFFC;
            if (pc_loaded) *cpu.cpsr = *cpu.spsr;
            if (!register_in_rlist) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            // @IF( W !L) cpu.regs[get_nth_bits(opcode, 16, 20)] = writeback_address;
            break;
        }
    default: break;
    }
}

void entry_10100000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10100001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10100010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10100011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10100100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10100101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10100110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10100111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10101000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10101001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10101010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10101011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10101100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10101101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10101110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10101111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10110000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10110001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10110010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10110011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10110100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10110101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10110110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10110111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10111000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10111001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10111010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10111011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10111100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10111101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10111110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_10111111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b0111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1000: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1001: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1010: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1011: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1100: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1101: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1110: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
        case 0b1111: {
            *cpu.lr = *cpu.pc - 8;
            // unintuitive sign extension: http://graphics.stanford.edu/~seander/bithacks.html#FixedSignExtend
            *cpu.pc += ((((1 << 23) ^ get_nth_bits(opcode, 0, 24)) - (1 << 23)) << 2) - 4;
            cpu.refill_pipeline();
            // _g_cpu_cycles_remaining += 3;
            break;
        }
    default: break;
    }
}

void entry_11000000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11000001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11000010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11000011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11000100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11000101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11000110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11000111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11001000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11001001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11001010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11001011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11001100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11001101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11001110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11001111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11010000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11010001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11010010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11010011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11010100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11010101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11010110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11010111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11011000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11011001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11011010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11011011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11011100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11011101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11011110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11011111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11100000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11100001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11100010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11100011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11100100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11100101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11100110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11100111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11101000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11101001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11101010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11101011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11101100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11101101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11101110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11101111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b0111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1000: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1001: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1010: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1011: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1100: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1101: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1110: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
        case 0b1111: {
            warning(format("No implementation for opcode 0x" ~ to_hex_string(opcode)));
            break;
        }
    default: break;
    }
}

void entry_11110000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void entry_11110001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void entry_11110010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void entry_11110011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void entry_11110100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void entry_11110101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void entry_11110110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void entry_11110111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void entry_11111000(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void entry_11111001(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void entry_11111010(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void entry_11111011(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void entry_11111100(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void entry_11111101(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void entry_11111110(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void entry_11111111(uint opcode, T cpu) {
    uint discriminator = (((opcode >> 0) & 0) << 0) | (((opcode >> 4) & 15) << 0);

    switch (discriminator) {
        case 0b0000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b0111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1000: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1001: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1010: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1011: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1100: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1101: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1110: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
        case 0b1111: {
            cpu.exception(CpuException.SoftwareInterrupt);
            break;
        }
    default: break;
    }
}

void execute_instruction(uint opcode, T cpu) {
    jumptable[(((opcode >> 0) & 0) << 0) | (((opcode >> 20) & 255) << 0)](opcode, cpu);
}

immutable jumptable = [
    &entry_00000000,
    &entry_00000001,
    &entry_00000010,
    &entry_00000011,
    &entry_00000100,
    &entry_00000101,
    &entry_00000110,
    &entry_00000111,
    &entry_00001000,
    &entry_00001001,
    &entry_00001010,
    &entry_00001011,
    &entry_00001100,
    &entry_00001101,
    &entry_00001110,
    &entry_00001111,
    &entry_00010000,
    &entry_00010001,
    &entry_00010010,
    &entry_00010011,
    &entry_00010100,
    &entry_00010101,
    &entry_00010110,
    &entry_00010111,
    &entry_00011000,
    &entry_00011001,
    &entry_00011010,
    &entry_00011011,
    &entry_00011100,
    &entry_00011101,
    &entry_00011110,
    &entry_00011111,
    &entry_00100000,
    &entry_00100001,
    &entry_00100010,
    &entry_00100011,
    &entry_00100100,
    &entry_00100101,
    &entry_00100110,
    &entry_00100111,
    &entry_00101000,
    &entry_00101001,
    &entry_00101010,
    &entry_00101011,
    &entry_00101100,
    &entry_00101101,
    &entry_00101110,
    &entry_00101111,
    &entry_00110000,
    &entry_00110001,
    &entry_00110010,
    &entry_00110011,
    &entry_00110100,
    &entry_00110101,
    &entry_00110110,
    &entry_00110111,
    &entry_00111000,
    &entry_00111001,
    &entry_00111010,
    &entry_00111011,
    &entry_00111100,
    &entry_00111101,
    &entry_00111110,
    &entry_00111111,
    &entry_01000000,
    &entry_01000001,
    &entry_01000010,
    &entry_01000011,
    &entry_01000100,
    &entry_01000101,
    &entry_01000110,
    &entry_01000111,
    &entry_01001000,
    &entry_01001001,
    &entry_01001010,
    &entry_01001011,
    &entry_01001100,
    &entry_01001101,
    &entry_01001110,
    &entry_01001111,
    &entry_01010000,
    &entry_01010001,
    &entry_01010010,
    &entry_01010011,
    &entry_01010100,
    &entry_01010101,
    &entry_01010110,
    &entry_01010111,
    &entry_01011000,
    &entry_01011001,
    &entry_01011010,
    &entry_01011011,
    &entry_01011100,
    &entry_01011101,
    &entry_01011110,
    &entry_01011111,
    &entry_01100000,
    &entry_01100001,
    &entry_01100010,
    &entry_01100011,
    &entry_01100100,
    &entry_01100101,
    &entry_01100110,
    &entry_01100111,
    &entry_01101000,
    &entry_01101001,
    &entry_01101010,
    &entry_01101011,
    &entry_01101100,
    &entry_01101101,
    &entry_01101110,
    &entry_01101111,
    &entry_01110000,
    &entry_01110001,
    &entry_01110010,
    &entry_01110011,
    &entry_01110100,
    &entry_01110101,
    &entry_01110110,
    &entry_01110111,
    &entry_01111000,
    &entry_01111001,
    &entry_01111010,
    &entry_01111011,
    &entry_01111100,
    &entry_01111101,
    &entry_01111110,
    &entry_01111111,
    &entry_10000000,
    &entry_10000001,
    &entry_10000010,
    &entry_10000011,
    &entry_10000100,
    &entry_10000101,
    &entry_10000110,
    &entry_10000111,
    &entry_10001000,
    &entry_10001001,
    &entry_10001010,
    &entry_10001011,
    &entry_10001100,
    &entry_10001101,
    &entry_10001110,
    &entry_10001111,
    &entry_10010000,
    &entry_10010001,
    &entry_10010010,
    &entry_10010011,
    &entry_10010100,
    &entry_10010101,
    &entry_10010110,
    &entry_10010111,
    &entry_10011000,
    &entry_10011001,
    &entry_10011010,
    &entry_10011011,
    &entry_10011100,
    &entry_10011101,
    &entry_10011110,
    &entry_10011111,
    &entry_10100000,
    &entry_10100001,
    &entry_10100010,
    &entry_10100011,
    &entry_10100100,
    &entry_10100101,
    &entry_10100110,
    &entry_10100111,
    &entry_10101000,
    &entry_10101001,
    &entry_10101010,
    &entry_10101011,
    &entry_10101100,
    &entry_10101101,
    &entry_10101110,
    &entry_10101111,
    &entry_10110000,
    &entry_10110001,
    &entry_10110010,
    &entry_10110011,
    &entry_10110100,
    &entry_10110101,
    &entry_10110110,
    &entry_10110111,
    &entry_10111000,
    &entry_10111001,
    &entry_10111010,
    &entry_10111011,
    &entry_10111100,
    &entry_10111101,
    &entry_10111110,
    &entry_10111111,
    &entry_11000000,
    &entry_11000001,
    &entry_11000010,
    &entry_11000011,
    &entry_11000100,
    &entry_11000101,
    &entry_11000110,
    &entry_11000111,
    &entry_11001000,
    &entry_11001001,
    &entry_11001010,
    &entry_11001011,
    &entry_11001100,
    &entry_11001101,
    &entry_11001110,
    &entry_11001111,
    &entry_11010000,
    &entry_11010001,
    &entry_11010010,
    &entry_11010011,
    &entry_11010100,
    &entry_11010101,
    &entry_11010110,
    &entry_11010111,
    &entry_11011000,
    &entry_11011001,
    &entry_11011010,
    &entry_11011011,
    &entry_11011100,
    &entry_11011101,
    &entry_11011110,
    &entry_11011111,
    &entry_11100000,
    &entry_11100001,
    &entry_11100010,
    &entry_11100011,
    &entry_11100100,
    &entry_11100101,
    &entry_11100110,
    &entry_11100111,
    &entry_11101000,
    &entry_11101001,
    &entry_11101010,
    &entry_11101011,
    &entry_11101100,
    &entry_11101101,
    &entry_11101110,
    &entry_11101111,
    &entry_11110000,
    &entry_11110001,
    &entry_11110010,
    &entry_11110011,
    &entry_11110100,
    &entry_11110101,
    &entry_11110110,
    &entry_11110111,
    &entry_11111000,
    &entry_11111001,
    &entry_11111010,
    &entry_11111011,
    &entry_11111100,
    &entry_11111101,
    &entry_11111110,
    &entry_11111111,
];
}